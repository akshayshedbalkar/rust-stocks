## Usage
Call `cargo run`. Resulting plot is saved as `Stocks.png`.  
After first run, `Stocks.toml` file is available to change parameters if necessary.

## API key
Provide Tiingo API key in a .env file `license="alphanurmeircValueHere"`.
